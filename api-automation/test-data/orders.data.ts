export const nonExistentOrderId = "ORD-999999";
export const invalidOrderIdFormat = "INVALID-ID";

// Order IDs are generated at runtime; these are used in negative tests only.
// Actual order IDs are obtained by placing orders in beforeAll/beforeEach hooks.
